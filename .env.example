# Required for Google Custom Search JSON API (available to existing customers only).
GOOGLE_SEARCH_API_KEY=
GOOGLE_SEARCH_ENGINE_ID=

# The API server listens here. Vite proxies /api requests to this address in development.
PORT=8787
