# Required for the server-side OpenAI web-search call. Never expose this in the React app.
OPENAI_API_KEY=
OPENAI_SEARCH_MODEL=gpt-5.4-mini

# The API server listens here. Vite proxies /api requests to this address in development.
PORT=8787
